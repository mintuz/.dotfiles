interface:
  display_name: "Decision Trace"
  short_description: "Trace decisions through delivery artifacts"
  default_prompt: "Use $decision-trace to follow this claimed decision from its source through specifications, issues, PRs, and commits."
